<match **>
  @type loki
  url "#{ENV['LOKI_USERNAME']}"
  username "#{ENV['LOKI_USERNAME']}"
  password "#{ENV['LOKI_PASSWORD']}"
  extra_labels {"machine":"#{ENV['MACHINE_NAME']}"}
  <buffer>
    flush_interval 10s
    flush_at_shutdown true
  </buffer>
  buffer_chunk_limit 1m
</match>
