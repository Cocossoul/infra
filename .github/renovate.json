{
  "enabledManagers": ["dockerfile", "ansible-galaxy", "terraform", "pip_requirements", "github-actions", "regex"],
  "regexManagers": [
    {
      "datasourceTemplate": "docker",
      "fileMatch": ["\\.tf"],
      "matchStrings": [
        "data \"docker_registry_image\" \".*\" {\\s*name = \"(?<depName>.*?):(?<currentValue>.*?)\""
      ]
    }
  ]
}
