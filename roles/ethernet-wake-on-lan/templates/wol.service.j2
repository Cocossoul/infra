[Unit]
Description=Wake-on-LAN for {{ wol_interface }}
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/sbin/ethtool -s {{ wol_interface }} wol g
Type=oneshot

[Install]
WantedBy=multi-user.target
