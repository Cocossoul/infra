---
traefik_user: "{{ lookup('ansible.builtin.env', 'TRAEFIK_USER', default='admin') }}"
traefik_password: "{{ lookup('ansible.builtin.env', 'TRAEFIK_PASSWORD', default='admin') }}"
domain: "{{ lookup('ansible.builtin.env', 'DOMAIN', default='corentinpape.com') }}"
